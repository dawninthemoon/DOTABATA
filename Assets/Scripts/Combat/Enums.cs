using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Combat
{
    public enum TargetFaction
    {
        None = -1,
        Character = 0,
        Enemy = 1,
    }
}
